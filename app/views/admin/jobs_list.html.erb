<div class="container py-5">
  <div class="row py-5 ">
    <div class="p-3 col-11 mx-auto tab-header d-flex align-item-center justify-content-between">
      <h3>All Jobs</h3>
    </div>
    <div class="p-3 col-11 mx-auto page-body">
      <div class="row p-0">
        <div class="col-4 ms-auto">
          <div class="input-group mb-3 ">
            <span class="input-group-text"><i class="fa-solid fa-magnifying-glass"></i></span>
            <input type="text" class="form-control" placeholder="search user here">
          </div>
        </div>
      </div>
      <div class="row p-0">
        <table class="table text-center table-striped">
          <thead class="bg-light">
            <tr>
              <th scope="col">#</th>
              <th scope="col">Job Title</th>
              <th scope="col">Request Send</th>
              <th></th>
            </tr>
          </thead>
          <tbody id="role_table">
            <% if !@jobs.blank? %>
              <% for job in @jobs %>
                <tr>
                  <td class="col"><%= job.id %></td>
                  <td class="col"><%= job.title %></td>
                  <td class="col">0</td>
                  <td class="col">
                    <%= link_to "Delete",job_path(job),method: :delete,class:'btn btn-danger'  %>
                  </td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<script>
  $('#jobs_list').addClass('active');
</script>
<!-- Button trigger modal -->
<!-- Modal -->
<div class="modal  fade" id="addRoleModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="addRoleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="addRoleModalLabel">Add New User Role</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <%= form_for(UserRole.new,remote: true)  do |f| %>
        <div class="modal-body">
          <div class="form-group">
            <label class="form-label"> Role Name </label>
            <%= f.text_field :role_name,class:"form-control" %>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <%= f.submit 'Add Role',class:'btn btn-success' %>
        </div>
      </form>
    </div>
  </div>
</div>
<% end %>
